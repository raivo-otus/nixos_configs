# This file must be encrypted before committing.
# After filling in .sops.yaml with real keys, run:
#   sops --encrypt --in-place secrets/grenth.yaml
hermes-agent-env: |
    OPENAI_API_KEY=local
open-webui-env: |
    WEBUI_SECRET_KEY=replace-with-a-random-string
